import { Typography } from '@mui/material';

export default function Page() {
  return (
    <main>
      <Typography variant="h2" gutterBottom>
        Welcome!
      </Typography>
      <Typography variant="body1">
        This is for practicing because I want to be better.
      </Typography>
    </main>
  );
}
